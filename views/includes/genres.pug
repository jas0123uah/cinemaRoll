
//mixin blockList(arrayOfGenres, divId)
//  div(id= divId)
//    each genre in arrayOfGenres
//      a(href= `/genres/`)=genre 
//      br

div(id="genre-container")
  h2(class="search-header" id="search-header") Browse movies by genre
  each genreArray, genreArrayIndex in genres
    div(id="genres-" + (genreArrayIndex + 1))
      each genre in genreArray
        a(href= "/genres/" + genre.name.replace(" ", "%20"))= genre.name
        br
